'use client'

import { Server } from "";

interface ServerHeaderProps {
    server: Server 
}

const ServerHeader = () => {
    return (
        <div>
            
        </div>
    );
};

export default ServerHeader;