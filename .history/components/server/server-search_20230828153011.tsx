interface ServerSearchProps {
    data: {
        label: string;
        type: 'channel' | 'member';
        data: {
            
        }
    };
}

const ServerSearch = () => {
    return <div></div>;
};

export default ServerSearch;
