'use client'

import { Server } from "pris";

interface ServerHeaderProps {
    server: Server 
}

const ServerHeader = () => {
    return (
        <div>
            
        </div>
    );
};

export default ServerHeader;