'use client';

import { Member } from '@prisma/client';

interface ChatMessagesProps {
    name: string;
    member: Member;
}

const ChatMessages = () => {
    return <div></div>;
};

export default ChatMessages;
