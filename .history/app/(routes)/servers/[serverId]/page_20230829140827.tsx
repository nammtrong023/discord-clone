interface ServerIdPageProps {
    params: { serverId: string };
}

export default function async ServerIdPage({ params }: ServerIdPageProps) {
    return <main className='min-h-full'>trang chu</main>;
}
