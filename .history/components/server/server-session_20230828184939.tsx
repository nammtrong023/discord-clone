import { MemberRole } from "@prisma/client";

interface ServerSessionProps {
    label: string,
    role?: MemberRole
    sections
}

const SeverSession = () => {
    return <div></div>;
};

export default SeverSession;
