/** @type {import('next').NextConfig} */
const nextConfig = {
    webpack: () => {
        config.ex
    }

    images: {
        domains: ['uploadthing.com'],
    },
};

module.exports = nextConfig;
