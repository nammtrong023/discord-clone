import { type } from "os";

interface ChatInputProps {
    apiUrl: string;
    query: Record<string, any>;
    name: string,
    type:'conversation' 
}

const ChatInput = () => {
    return <div></div>;
};

export default ChatInput;
